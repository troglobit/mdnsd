EXTRA_DIST         = lib.sh discover.sh
CLEANFILES         = *~ *.trs *.log
SUBDIRS            = src

TEST_EXTENSIONS    = .sh
TESTS_ENVIRONMENT  = unshare -mrun

TESTS              = discover.sh iprecords.sh
